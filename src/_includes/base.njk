<!doctype html>
<html lang="{{ metadata.lang }}">

<head>
    {% include "meta.njk" %}
</head>

{# Push footer to bottom #}
<body class="flex flex-col min-h-screen text-text bg-background dark:bg-background-dark dark:text-text-dark">
    <div class="flex-auto">
        {% include "header.njk" %}
        <main>
            {{ content | safe }}
        </main>
    </div>
    {% include "footer.njk" %}
</body>

</html>
